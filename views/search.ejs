<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search - Social Network for Music</title>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>Search</h1>
        </header>
        <main>
            <!-- Search form for playlists -->
            <form action="/search" method="get">
                <label for="searchQuery">Search Playlists:</label><br>
                <input type="text" id="searchQuery" name="searchQuery" required><br>
                <button type="submit">Search</button>
            </form>
            <div class="search-results">
                <!-- Search results will be displayed here -->
                <% if (playlists.length > 0) { %>
                    <h2>Playlists found:</h2>
                    <ul>
                        <% playlists.forEach(playlist => { %>
                            <li><%= playlist.title %></li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <p>No playlists found.</p>
                <% } %>
            </div>
        </main>
        <footer class="footer">
            <p>&copy; 2024 Social Network for Music</p>
        </footer>
    </div>
</body>
</html>
